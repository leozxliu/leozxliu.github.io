---
layout: default
---
{%- comment -%}
  Local override of al_folio_core's about layout.
  Adds a two-column hero, a blood-flow band, and list-style news.
{%- endcomment -%}
<div class="post home">
  <section class="hero-full" aria-label="{{ page.hero.band_alt }}">
    <div class="hero-media">
      <img src="{{ '/assets/img/banners/home.jpg' | relative_url }}" alt="" loading="eager">
    </div>
    <div class="hero-top">
      <h1 class="hero-name">{{ site.first_name }} {{ site.last_name }}</h1>
      <p class="hero-sub">{{ page.hero.eyebrow }}</p>
    </div>
    <div class="hero-bottom">
      {% for a in page.hero.actions limit: 2 %}
        <a class="btn-t{% if forloop.first %} is-primary{% else %} is-light{% endif %}" href="{{ a.url | relative_url }}">{{ a.label }}</a>
      {% endfor %}
    </div>
  </section>
  <p class="hero-caption">{{ page.hero.band_caption }}</p>

  <section class="intro">
    <figure class="intro-portrait">
      {% assign profile_image_path = page.profile.image | prepend: 'assets/img/' %}
      {% include figure.liquid loading="eager" path=profile_image_path class="img-fluid" sizes="(min-width: 768px) 280px, 90vw" alt=page.profile.alt cache_bust=true %}
      <figcaption>{{ page.profile.more_info }}</figcaption>
    </figure>
    <div class="intro-text">
      <p class="intro-role">{{ page.subtitle }}</p>
      <p class="intro-thesis">{{ page.hero.thesis }}</p>
      {{ content }}
    </div>
  </section>

  <article>
    {% if page.announcements and page.announcements.enabled %}
      {% assign news_size = site.news | size %}
      <section class="home-section">
        <div class="section-head">
          <h2>News</h2>
          <a class="section-link" href="{{ '/news/' | relative_url }}">All {{ news_size }} items →</a>
        </div>
        <ol class="news-list{% if page.announcements.scrollable %} is-scrollable{% endif %}">
          {% assign news = site.news | reverse %}
          {% if page.announcements.limit %}{% assign news_limit = page.announcements.limit %}{% else %}{% assign news_limit = news_size %}{% endif %}
          {% for item in news limit: news_limit %}
            <li>
              <time datetime="{{ item.date | date: '%Y-%m-%d' }}">{{ item.date | date: '%b %Y' }}</time>
              <div class="news-body">
                {% if item.inline %}{{ item.content | emojify }}{% else %}<a class="news-title" href="{{ item.url | relative_url }}">{{ item.title }}</a>{% endif %}
              </div>
            </li>
          {% endfor %}
        </ol>
      </section>
    {% endif %}

    {% if page.selected_papers %}
      <section class="home-section">
        <div class="section-head">
          <h2>Selected publications</h2>
          <a class="section-link" href="{{ '/publications/' | relative_url }}">Full list →</a>
        </div>
        {% include selected_papers.liquid %}
      </section>
    {% endif %}

    {% if page.social %}
      <div class="social">
        <div class="contact-icons">{% social_links %}</div>
        <div class="contact-note">{{ site.contact_note }}</div>
      </div>
    {% endif %}
  </article>
</div>

